{
  "name": "brocade-education",
  "version": "1.0.0",
  "private": true,
  "description": "锦泓留学 Brocade Education 咨询平台原型",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "node -e \"const fs=require('fs');fs.rmSync('dist',{recursive:true,force:true});fs.cpSync('public','dist',{recursive:true});if(!fs.existsSync('dist/index.html'))throw new Error('dist/index.html was not generated');console.log('Static build created in dist')\"",
    "start": "node --env-file-if-exists=.env server.js"
  }
}
